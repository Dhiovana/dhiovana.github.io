function sum(num1, num2) {
    var DECIMAL = 10;
    return parseInt(num1, DECIMAL) + parseInt(num2, DECIMAL);
}

-checar num(numEntrada);
numEntrada = parseFloat(numEntrada);
if (isNaN(numEntrada)) return 0;
return numEntrada;

function maior(num1, num2) {
    if (n1 > n2)
        return n1
    else
        return n2
} 